#include <iostream>
#include <deque>
#include <queue>
#include <string>

using namespace std;

int main() {
    int n;cin >> n;
    vector<string> v(n);
    for (int i = 0 ; i < n ; i++) cin >> v[i];
    while (!v.empty()) {
        int i = v.size() - 1;
        //to be continue;
    }
    
}